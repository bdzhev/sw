# sw

## Agent skills

### Issue tracker

Issues live in GitHub Issues at `bdzhev/sw`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Multi-context — `CONTEXT-MAP.md` at the root points at one `CONTEXT.md` per side
(`frontend/`, `backend/`). See `docs/agents/domain.md`.
