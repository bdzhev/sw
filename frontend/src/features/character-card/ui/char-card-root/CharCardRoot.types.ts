import type { CharacterSummary } from '@shared/api/characters';

/** The summary every card part reads through inject. */
export type CharCardContext = CharacterSummary;

export type CharCardRootProps = CharCardContext;
