export interface DialogContentProps {
  /**
   * Keeps the dialog open on an outside click. Use it while an action started
   * from inside the dialog is still in flight.
   */
  disableOutsideClose?: boolean;
}
