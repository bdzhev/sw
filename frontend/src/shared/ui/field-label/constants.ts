/**
 * The one field-label style. Exported as a string for the single caller that
 * cannot use `FieldLabel` itself: a `<fieldset>`'s caption must be a `<legend>`,
 * which is not a `<label>`.
 */
export const FIELD_LABEL_CLASSES = 'text-xs text-secondary uppercase';
