// import { useRoute } from 'vue-router';

// import type { useChangeLanguageOptions } from './useChangeLanguage.types';

// import { useI18n } from 'vue-i18n';

// export const useChangeLanguage = (options: useChangeLanguageOptions) => {
//   const { locale } = options;

//   const i18n = useI18n();

//   const { name } = useRoute();

//   const changeLanguage = async () => {
//     if (name) {
//     }
//   };

//   return { changeLanguage };
// };
