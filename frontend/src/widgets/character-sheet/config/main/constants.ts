/**
 * The ability descriptors themselves live in `@entities/characters` — the skills
 * tab needs the same labels, and they were briefly defined in both places.
 * Only the editing bounds are local to this tab.
 */
export const MIN_ABILITY_SCORE = 1;
export const MAX_ABILITY_SCORE = 30;
