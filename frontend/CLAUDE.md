# Frontend

**Read [rules.md](./rules.md) in full before writing, moving, or deleting any file in `frontend/`.** It is the single source of truth for conventions — FSD layers, component folder structure, Tailwind v4 setup, tooling. Do not infer conventions from surrounding code; much of it is legacy that `rules.md` explicitly supersedes.
